# apps/auth/Dockerfile.dev

FROM node:18-alpine

WORKDIR /app

RUN apk add --no-cache git

COPY package*.json ./
COPY nx.json ./
COPY tsconfig.base.json ./

RUN npm ci

COPY . .

EXPOSE 3001

ENV NODE_ENV=development
ENV PORT=3001

CMD ["npx", "nx", "serve", "auth", "--host=0.0.0.0", "--port=3001"]